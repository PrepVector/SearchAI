fastapi>=0.110
uvicorn>=0.29
# plain uvicorn on purpose: uvicorn[standard] pulls in watchfiles/httptools,
# which need a Rust + MSVC toolchain to build on very new Python versions
# (e.g. 3.14 free-threaded). SEARCH AI runs reload-free, so it does not
# need them. Pure-Python uvicorn installs everywhere.
pydantic>=2.6
httpx>=0.27
python-dotenv>=1.0
python-docx>=1.1

# --- optional extras (uncomment to enable) -----------------------------
# weasyprint>=61      # backend PDF fallback (primary PDF export is in-browser)
# cairosvg>=2.7       # embeds SEARCH AI SVG fallback visuals into Word exports
# Pillow>=10.0        # extra image format support for Word exports
