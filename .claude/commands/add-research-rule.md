---
description: Add a standing research or content rule (source preferences, format habits, banned patterns) to the knowledge base.
argument-hint: <the rule, in plain language>
---

Rule: $ARGUMENTS

Add this as a standing rule to the knowledge base. First figure out which
file it belongs in — ask the person if it's genuinely ambiguous, otherwise
use your judgement:

- **Source-related** (what counts as credible, what to avoid, domain-
  specific sourcing rules, recency requirements) → `knowledge_base/source_rules.md`
- **Content/format-related** (formatting habits, banned phrases,
  structural preferences, layout defaults) → `knowledge_base/content_rules.md`
- **Depth/audience/general defaults** → `knowledge_base/research_profile.md`

Read the target file first, then add the rule under the appropriate
existing section (don't create a parallel new section if an existing one
already fits — e.g. a new banned phrase goes under content_rules.md's
"Banned patterns" section, not a new one). Replace a placeholder "(None
set yet...)" line if that section was still empty; otherwise append to the
existing list.

Keep the rule's own wording close to what the person said — don't
over-formalize a simple instruction into corporate-sounding policy
language.

Confirm which file you added it to and show the updated section.
